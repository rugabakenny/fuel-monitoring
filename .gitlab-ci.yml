test:
    - apt-get install -y python-dev python-pip
  - pip install -r requirements.txt
  - python manage.py test

staging:
  type: deploy
  script:
  only:
  - master

production:
  type: deploy
  script:

  only:
  - tags

